fullnameOverride: devops-showcase-backend
image:
  repository: matejcihlar/devops-project-backend
  tag: latest
  pullPolicy: Always

replicaCount: 1

resources:
  limits:
    cpu: 300m
    memory: 1024Mi
  requests:
    cpu: 300m
    memory: 512Mi

service:
  type: ClusterIP
  port: 90